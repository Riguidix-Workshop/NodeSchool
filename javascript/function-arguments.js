/**
 * @param {Number} x 
 * @param {Number} y 
 * @param {Number} z 
 * @returns {Number} The product of the y and z (params) in addition to the x (param)
 */
function math (x, y, z) {
    return x + (y * z);
}

console.log(math(53, 61, 67));